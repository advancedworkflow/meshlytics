{
  "name": "@meshlytics/tests-integration",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-package tests exercising the discovery -> engine -> instrumentation pipeline together.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@meshlytics/discovery": "workspace:*",
    "@meshlytics/engine": "workspace:*",
    "@meshlytics/instrumentation": "workspace:*",
    "@meshlytics/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
