# apps/playground

**Placeholder.** Intended as a richer, ever-growing Next.js app for
maintainers to manually exercise `meshlytics scan` / `instrument`
against realistic route structures (route groups, catch-alls, nested
layouts) as the engine grows.

Until this is built out, use the smaller [`examples/nextjs-app`](../../examples/nextjs-app)
for manual testing:

```
pnpm --filter example-nextjs-app dev
```
