# apps/docs

**Placeholder.** This will become the public documentation site
(likely Next.js + a docs framework such as Fumadocs), rendering the
markdown sources kept in the root [`docs/`](../../docs) directory.

Not built yet — contributions welcome.
